var countSheep = function (num)
{
    let count = "";
    if (num)
    {
        for (let i = 1; i <= num; i++)
        {
           count += i + " sheep..."
    
        }
        
    }
    return count;
}